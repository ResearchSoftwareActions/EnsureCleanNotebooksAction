name: 'Python Notebook Metadata Linter'
description: 'Applying a linting check to Python Notebook Metadata.'
inputs:
  lint-args:
    description: 'Arguments to pass to the Notebook metadata linting.'
    required: false
    default: ''
runs:
  using: 'node12'
  main: 'dist/index.js'
